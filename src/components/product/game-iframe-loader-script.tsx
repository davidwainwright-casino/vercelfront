import Script from 'next/script'

interface Props {
  game_id: string;
  real_url: string;
}

export default function GameIframeLoaderScript({
  game_id,
  real_url,
}: Props) {
  return (
        <>
        </>
  );
}
